plugin:
  grpc:
    config:
      namespace: ${AB_NAMESPACE:accelbyte}
      resource_name: 'MMV2GRPCSERVICE'
    server:
      interceptor:
        debug-logger:
          enabled: false
        auth:
          enabled: true